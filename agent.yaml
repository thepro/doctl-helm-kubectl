apiVersion: v1
kind: Pod
metadata:
  labels:
    pod-name: do-k8-agent-pod
spec:
  containers:
    - name: docker
      image: docker:dind
      securityContext:
        privileged: true
      envFrom:
        - secretRef:
            name: cd-secrets
        - configMapRef:
            name: cd-config
      command:
        - cat
      tty: true
